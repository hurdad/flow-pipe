flow-pipe installed.

Controller:
  No ports are exposed for the controller.

API:
  HTTP:
    kubectl port-forward svc/flow-pipe-api {{ .Values.api.httpPort }}
  gRPC:
    kubectl port-forward svc/flow-pipe-api {{ .Values.api.grpcPort }}

Runtime pods are created dynamically by the controller.
