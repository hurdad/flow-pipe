flow-pipe installed.

Controller:
  kubectl port-forward svc/flow-pipe-controller {{ .Values.controller.port }}

API:
  kubectl port-forward svc/flow-pipe-api {{ .Values.api.port }}

Runtime pods are created dynamically by the controller.
